nslookup slash16.org


#dig @8.8.8.8 slash16.org !!
#man nslookup
#тоже самое, что и пятом задании, только добавить 8.8.8.8:
#dig slash16.org | awk ' /'SERVER'/ {print $0;} '
#печатает только строку , которая начинается с SERVER (то есть какой у сервера IP адрес)
#dig slash16.org 
#петатает больше информации и о других адресах (А)
#dig +short slash16.org 
#выводить только А-адреса
#или через nslookup (только восьмерки без собаки):
#



!!!!!! nslookup slash16.org 8.8.8.8
